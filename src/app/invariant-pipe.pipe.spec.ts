import { InvariantPipePipe } from './invariant-pipe.pipe';

describe('InvariantPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InvariantPipePipe();
    expect(pipe).toBeTruthy();
  });
});
